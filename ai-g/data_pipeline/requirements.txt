# LDAR AI-G 데이터 수집 파이프라인
# capture.py / extract_frames.py 실행용 (학습용 PC 또는 촬영 Pi)
#
# picamera2 는 Raspberry Pi OS 에서 apt 로 설치하는 게 정석:
#   sudo apt install -y python3-picamera2
# (pip 설치는 비권장 — libcamera 바인딩 때문) picamera2 가 없으면 코드가
# 자동으로 OpenCV(/dev/video0) 폴백으로 동작한다.

numpy>=1.24
opencv-python>=4.8
